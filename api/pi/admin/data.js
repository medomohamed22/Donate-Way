const {getAdmin,headers,buildQuery}=require('./_shared.cjs');
const TABLES=new Set(['campaigns','donations','campaign_updates','campaign_comments','admin_activity_logs']);
module.exports=async function(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method Not Allowed'});try{if(!await getAdmin(req))return res.status(401).json({error:'Unauthorized'});const q=req.body||{},table=q.table;if(!TABLES.has(table))return res.status(403).json({error:'Table not allowed'});let url=buildQuery(table,q);if(q.onConflict)url+=`&on_conflict=${encodeURIComponent(q.onConflict)}`;let method='GET',body,prefer='return=representation';if(q.op==='insert'||q.op==='upsert'){method='POST';body=JSON.stringify(q.values);if(q.op==='upsert')prefer+=',resolution=merge-duplicates'}else if(q.op==='update'){method='PATCH';body=JSON.stringify(q.values)}else if(q.op==='delete'){method='DELETE'}else if(q.op!=='select')return res.status(400).json({error:'Invalid operation'});const r=await fetch(url,{method,headers:headers({Prefer:prefer}),body});if(!r.ok)return res.status(r.status).json({error:await r.text()});const text=await r.text();let data=text?JSON.parse(text):[];if(q.single)data=Array.isArray(data)?(data[0]||null):data;return res.status(200).json({data})}catch(e){return res.status(500).json({error:e.message||'Admin data request failed'})}};
